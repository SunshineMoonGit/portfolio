{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build:content": "node --import tsx scripts/build-content.ts",
    "dev:web": "pnpm --filter web dev",
    "check:web": "pnpm --filter web check",
    "build:web": "pnpm --filter web build",
    "check:paths": "bash scripts/check-base-path.sh"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
